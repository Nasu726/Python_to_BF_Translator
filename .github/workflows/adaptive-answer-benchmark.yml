name: experimental-benchmark

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ad-hoc performance experiments now live in the main runtime telemetry or git history."
